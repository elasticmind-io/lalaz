<?php

return [
    // Define the current env of the app
    'env' => env('ENV', 'development'),

    // Define the app name
    'name' => env('APP_NAME', 'CodeInit'),

    // Define the debug mode for the app
    'debug' => (bool)env('APP_DEBUG', false),
];
